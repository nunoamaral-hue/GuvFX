/**
 * DEPRECATED — Replaced by the full onboarding flow at /onboarding.
 *
 * The backend-authoritative onboarding system now lives in:
 *   - frontend/src/components/onboarding/OnboardingShell.tsx
 *   - frontend/src/app/(app)/onboarding/page.tsx
 *
 * This file is retained as a no-op to avoid broken imports if
 * any future code references it. It renders nothing.
 */
export function OnboardingChecklist() {
  return null;
}
