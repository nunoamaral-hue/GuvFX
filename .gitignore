# Python
backend/.venv/
backend/__pycache__/
backend/**/*.pyc

# Research environment (GFX-PKT-005B) — local, never staged
/.venv-research/
/research-data/

# Django DB
backend/db.sqlite3
backend/wims_demo.sqlite3
backend/flow_a_shadow.sqlite3

# Next.js / Node
frontend/node_modules/
frontend/.next/
frontend/.turbo/

# Logs
*.log

# OS
.DS_Store

# Other
.env
backend/.env
frontend/.env
*.env
GuvPay-pr/

# Local cleanup trash + accidental duplicate files
.trash_duplicates/
* 2.py
* 2.ts
* 2.tsx
* 2.json
* 2.md
# .ps1 was missing from this list. Duplicates of install_pool.ps1 / install_service.ps1 /
# uninstall.ps1 appeared in deploy/beta-agent and showed as UNTRACKED rather than ignored - one
# `git add -A` away from committing a second, unreviewed copy of a credentialed installer.
* 2.ps1
* 3.py
* 3.ps1
* 3.ts
* 3.tsx
* 3.json
* 3.md
# Accidental numbered duplicates (Finder copies)
* [0-9].py
* [0-9].ts
* [0-9].tsx
* [0-9].json
* [0-9].md

# Accidental numbered duplicates like "eslint (1).config.mjs"
* ([0-9]).py
* ([0-9]).ts
* ([0-9]).tsx
* ([0-9]).json
* ([0-9]).md
* ([0-9]).config.mjs

# Python cache
__pycache__/
*.pyc

# Local Claude material — ignore specific local files only.
# Do NOT ignore the whole .claude/ directory: .claude/rules/*.md stay tracked.
/.claude/settings.local.json
/.claude/launch.json
/.claude/px24_dashboard_final_20260611.tsx
/.claude/px24_rx2_merged_dashboard_20260611.tsx
/.claude/px24_rx2_status_cleanup_20260611.tsx

# Wayond certification draft staging files (drafts, never committed)
wayond_staging.json
**/wayond_staging*.json
