# Beta-Agent Phase-3 Certification — 2026-07-30

**Scope:** the Sponsor-authorised minimum read-only certification work required to reach Phase 4:
(1) host RULE-9 parse gate; (2) WinSW service recertification through the remediated installer;
(3) service identity / ACL / firewall / bind verification; (4) clean RULE-10 golden verification;
(5) Phase-3 certification verdict.

**Posture:** strictly READ-ONLY. No host mutation, no service stop/start/reinstall, no signing-key
provisioning, no Customer-Zero change, ProvisioningJob #1 untouched, no residual-DLL mutation, no secret
displayed. Host = `WIN-RD8VDS93DK7` @ `100.79.101.19` (Tailscale), via `ssh administrator@… powershell
-NoProfile -EncodedCommand`. All commands were pure `Get-*` / `icacls` / `Test-Path` / `[Parser]::ParseFile`,
plus the two sanctioned non-mutating installer modes (`-VerifyOnly`), which self-report *"Nothing was created,
changed or started by this run."*

Governance note carried into the verdict: `install_service.ps1` has **no** read-only verify mode — its VERIFY
block is the tail of `-Apply` and hard-asserts the service is **Stopped / ProcessId 0** (`install_service.ps1:337,343`),
so it would throw against the live running service. Item (2) was therefore satisfied by **independently
reproducing each installer VERIFY assertion read-only**, inverting only the Stopped→Running expectation.

---

## Result summary

| # | Certification item | Result |
|---|---|---|
| 1 | RULE-9 host parse gate (6 `.ps1`, PS 5.1) | **PASS** |
| — | Bundle freshness (host == `main`) | **PASS** (byte-identical) |
| 2 | WinSW service recert (installer VERIFY assertions, read-only) | **PASS** |
| 3a | Service identity / start-mode / recovery / SeServiceLogonRight | **PASS** |
| 3b | Scoped NTFS ACLs (beta tree) + estate isolation | **PASS** |
| 3c | Firewall (scoped Allow + overriding Block, Private profile) | **PASS** (live reachability probe = post-start item) |
| 3d | Bind pin (`100.79.101.19:8791` exact, single listener) | **PASS** |
| 4 | RULE-10 golden — structure / cleanliness | **PASS** |
| 4 | RULE-10 golden — **digest matches pinned value** | **FAIL — golden drifted** |
| 5 | **Phase-3 verdict** | **CONDITIONAL — one blocker (golden digest drift)** |

---

## 1. RULE-9 host parse gate — PASS

`[System.Management.Automation.Language.Parser]::ParseFile` under **PS 5.1.26100.32522** on all six scripts:
`install_pool.ps1`, `install_service.ps1`, `firewall.ps1`, `uninstall.ps1`, `provision_beta_venv.ps1`,
`slot_launch.ps1` — **0 parse errors each**.

RULE-11 controls fired (so the zeros are trustworthy, not a silent encoding failure):
- In-memory broken input → **3** errors (error-detection path works).
- Real non-PS file `config.py` via `ParseFile` → **26** errors (file-read + error path works).

**Bundle freshness:** all 24 `.ps1`/`.py` first-16 SHA-256 on the host are **byte-identical to current `main`**
(`git archive main deploy/beta-agent`). No re-stage needed; the parse certifies current-main scripts.

## 2 + 3a. Service recertification — PASS

Installer VERIFY assertions (`install_service.ps1:336-372`) reproduced read-only against the **running** service:

| Assertion | Expected (install-gate) | Observed (live) | Verdict |
|---|---|---|---|
| StartName | `NT SERVICE\GuvFXBetaAgent` (reject LocalSystem/…) | `NT SERVICE\GuvFXBetaAgent` | PASS |
| State / ProcessId | Stopped / 0 *(fresh-install gate)* | **Running / 7656** *(live cert — expected inverse)* | PASS |
| StartMode | Manual | Manual | PASS |
| PathName | `…\agent-winsw\GuvFXBetaAgent.exe` | matches WinSW wrapper | PASS |
| Recovery (`sc qfailure`) | no RESTART/RUN/REBOOT actions | none (RESET_PERIOD 86400 + empty labels only) | PASS |
| SeServiceLogonRight | present for derived service SID | present + contains SID `S-1-5-80-1273106735-…-2795568148` | PASS |

RULE-11 on the rights check: secedit read with `-Encoding Unicode`; **positive control** `SeBatchLogonRight`
matched first (proving the parser finds a present right), so the `SeServiceLogonRight=present` result is trustworthy.
(An earlier loose-regex "recovery BAD" was a false positive matching the `REBOOT_MESSAGE` *label*; the raw
`sc qfailure` dump showed no configured actions.)

## 3b. NTFS ACLs — PASS

| Path | Expected | Observed | Verdict |
|---|---|---|---|
| `…\beta\agent` | service **RX** only | `NT SERVICE\GuvFXBetaAgent:(OI)(CI)(RX)` + Admin/SYSTEM F | PASS |
| `…\beta\agent-state` | service **Modify** | `(OI)(CI)(M)` + Admin/SYSTEM F | PASS |
| `…\beta\slots` (root) | service Modify, no slot ACE | `(OI)(CI)(M)` + Admin/SYSTEM F | PASS |
| `…\beta\slots\1` | `guvfx_b_slot1` Modify, **no other slot** | slot1 `(M)`; no slot2/3/4; service also F (tree manager) | PASS |
| `…\beta\tombstones` | Admin+SYSTEM Full + service Modify, inheritance broken | exactly those three, all explicit | PASS |
| `C:\GuvFX\accounts` (estate) | **no** beta ACE | svcSID=False, GuvFXBetaAgent=False, slot=False | PASS |
| `C:\GuvFX\terminals` (estate) | **no** beta ACE | svcSID=False, GuvFXBetaAgent=False, slot=False | PASS |
| `C:\GuvFX\golden\newMT5` | inheritance broken; Admin+SYSTEM Full; all non-admin **RX only** | protected=True; slots1-4 + service all `(OI)(CI)(RX)`; no write ACE | PASS |

## 3c. Firewall — PASS (live reachability probe deferred to post-start)

- `GuvFX-Beta-Agent-In`: Enabled, **Allow**, TCP 8791, local `100.79.101.19`, remote **`100.119.23.29` only**, Private profile.
- `GuvFX-Beta-Agent-Block-NonBackend`: Enabled, **Block**, TCP 8791, local `100.79.101.19`, remote = every IPv4 **except** the backend (`0.0.0.0-100.119.23.28`, `100.119.23.30-255.255.255.255`), Private.
- Interface `100.79.101.19` category = **Private** (never Public). Profile `DefaultInboundAction = NotConfigured` (resolves to Block for inbound; the scoped Block rule is the real protection).
- **B-8:** the only enabled inbound Allow rules that cover `:8791` by port are default Windows UWP/app rules and other-program rules (`vncviewer.exe`, `metatester64.exe`) — **none scoped to the agent's python** — and all are overridden for `:8791`-from-non-backend by the explicit Block rule (Block > Allow).
- **Completion item:** a live TCP probe of `100.79.101.19:8791` from a second tailnet peer (refused) and from the backend (allowed) is the definitive network-isolation proof and is a post-start step (INSTALL_REVIEW §5).

## 3d. Bind pin — PASS

`Get-NetTCPConnection -LocalPort 8791 -State Listen` → **exactly one** listener, `LocalAddress 100.79.101.19`
(not `0.0.0.0` / `::` / loopback), owning pid 13532 (the agent's venv python child of WinSW). Corroborated by
`agent.log` `bind=100.79.101.19:8791`.

## 4. RULE-10 golden — structure PASS, **digest FAIL (drift)**

`install_pool.ps1 -VerifyOnly -GoldenDir C:\GuvFX\golden\newMT5` (sanctioned, non-mutating — ended
*"pool VERIFIED. Nothing was created, changed or started by this run"*; all five estate tasks confirmed unchanged):

**Structure / cleanliness — PASS:** golden at `C:\GuvFX\golden\newMT5`; `terminal64.exe` FileVersion **5.0.0.6036**;
`.guvfx_golden_manifest` = `5.0.0.6036`; `.guvfx_portable` present; non-portable (no `MQL5\` at root → EA checks
vacuously clean); **no** `config\accounts.dat` / `servers.dat` / `common.ini` / `terminal.ini` / `origin.txt`;
**584** files; `bases\` = only `Default`, **537** files, no broker dir; golden ACL inheritance broken, all
non-admin RX-only. "golden image validated: clean, versioned, correctly structured."

**Digest — FAIL:** installer-computed tree digest =
`8a6480f40ce8340b3d65e6a106a21ab0d4bce0a18d9eb52ef75d9515a51c679b` (independently reproduced byte-for-byte by a
read-only reconstruction of `win_slot_ops.tree_digest()` — RULE-11 positive control), which **differs** from the
pinned `BETA_AGENT_GOLDEN_DIGEST = 3a7fa6638e9eb9a0989edcaaff5b0c9ec93b15a6c62b9ee9b5f5f420d6313f10`
(`config.example.json:16`). The installer's own output: *"a difference means the image changed — STOP."*

**Root cause (direct, mtime evidence):** `metatester64.exe` and `MetaEditor64.exe` were modified **2026-07-27
11:54** — four days after the 07-23 staging; every other file is dated 07-23. Two golden binaries changed in
place, same file count (584), so the tree digest changed while structure/cleanliness stayed valid.

**Corroborating isolation concern:** a Windows firewall rule registers
`C:\GuvFX\golden\newMT5\metatester64.exe` as the live "MetaTrader 5 Strategy Tester Agent" — evidence the golden's
binaries have been **launched/used**, which is in tension with RULE-10's "dedicated clean install, never
runtime-used." (Mechanism of the 07-27 change — MT5 self-update of the tester vs. shared use with the `:8787`
backtest agent — is undetermined read-only and needs investigation. `terminal64.exe` itself is unchanged.)

---

## 5. Phase-3 certification verdict — CONDITIONAL: one blocker

**PASS:** RULE-9 parse; bundle freshness; service identity/state/recovery/rights; scoped ACLs; estate isolation;
firewall rule configuration; bind pin; golden structure/cleanliness. The installer code is current-main and the
running service is correctly installed under the least-privilege virtual account.

**BLOCKER (must resolve before Phase 4):** the golden image at `C:\GuvFX\golden\newMT5` has **drifted from its
pinned digest** (`8a6480f4…` on disk vs `3a7fa663…` pinned; `metatester64.exe`+`MetaEditor64.exe` changed 07-27).
A `MATERIALISE` would fail `source_digest_matches` and be BLOCKED, and the golden shows evidence of runtime use.

**Remediation options (each a gated host mutation — Red / Sponsor):**
1. Investigate the 07-27 binary change: determine whether the golden is shared with the `:8787` backtest Strategy
   Tester Agent (firewall rule) and whether the change was a legitimate clean MT5 tester update.
2. Either **re-pin** `BETA_AGENT_GOLDEN_DIGEST` to `8a6480f4…` *after* confirming the change is a clean,
   intended MT5 build update and the image remains pristine; **or** **re-stage** a fresh dedicated clean golden
   isolated from backtest use (RULE-10 "never promote a used install").

**Non-blocking completion items:** live second-peer firewall reachability probe (post-start); residual pywin32
DLLs handled under the accepted operational exception (see `docs/OPERATIONAL_EXCEPTIONS.md`).

**STOP.** Phase 4 (keyring provisioning + Customer-Zero re-drive) remains separately gated and was not touched.
