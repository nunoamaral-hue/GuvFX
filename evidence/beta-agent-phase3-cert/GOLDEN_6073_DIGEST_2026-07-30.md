# Candidate Golden — Digest Record (SOP-001 Procedure 3, 2026-07-30)

Authorised scope: re-stage corrected validator → create markers → generate canonical digest → record → STOP.
No archive, promotion, rename, ACL, pin, or estate change. The old golden (`newMT5`, pid 5912) is untouched;
`BETA_AGENT_GOLDEN_DIGEST` is unchanged.

## Recorded metadata (canonical Golden identity)
| Field | Value |
|---|---|
| Candidate Golden path | `C:\GuvFX\golden\staging` |
| **MT5 build** | **5.0.0.6073** (terminal64/metatester64/MetaEditor64 coherent; Authenticode Valid `CN=MetaQuotes Ltd.`) |
| **Manifest version** (`.guvfx_golden_manifest`) | `5.0.0.6073` (10 bytes, ASCII, no BOM) |
| **File count** | **585** (583 installer files + 2 markers) |
| **Canonical tree digest** | `db54d94a51e18b9f2d592042e7bddb2a11eb576a6a927ea15ab7eab0a6509db0` |
| Digest algorithm | SHA-256 over the canonical per-file manifest (`normalise(relpath)\|size\|sha256`, ordinal-sorted) — install_pool.ps1 lines 1223-1238, verbatim; identical to `win_slot_ops.tree_digest` |
| Re-staged validator SHA-256 | `install_pool.ps1` = `038D04ED0EFD0A0BD613A55F488DAB7C8C86E155C3D88995BBD578EA131AF351` (host == local, byte-identical; RULE-9 parse 0 errors) |
| Archive SHA-256 | *deferred to the Archive step (not authorised in Procedure 3)* |

## Marker creation evidence
- `.guvfx_golden_manifest` = `5.0.0.6073` (10 bytes, ASCII no-BOM) — matches `terminal64.exe` FileVersion exactly.
- `.guvfx_portable` = empty (0 bytes).

## Sanctioned verification result — `install_pool.ps1 -ValidateGoldenOnly -GoldenDir C:\GuvFX\golden\staging`
```
ok  structure: terminal64.exe present
ok  structure: no MQL5 (non-portable install; /portable creates it in the slot at first run)
ok  marker: .guvfx_golden_manifest present
ok  marker: .guvfx_portable present
ok  version: terminal64.exe 5.0.0.6073 matches the pinned build
ok  clean: bases\ holds only the shipped Default tree (537 installer files, no broker directory)
ok  provenance: 558 scanned file(s) contain no path from another runtime or user profile
ok  golden image validated: clean, versioned, correctly structured
    ValidateGoldenOnly: stopping here. Nothing else was inspected, created or modified.
```
- **`config\servers.dat` accepted** (corrected validator; installer-shipped reference data) — the validation
  PASSED with it present, proving the on-host validator fix is effective.

## Not done in Procedure 3 (later, separately-authorised)
Golden-ACL / inert-in-place verification (that is `-VerifyOnly`, which asserts inheritance-broken + no-non-admin-write
— applied only at **promotion**, not authorised now); archive; promotion (rename/replace old golden, sever pid 5912,
remove tester firewall rule); `BETA_AGENT_GOLDEN_DIGEST` re-pin. The tree digest is invariant under ACL changes, so
`db54d94a…` is the value `-VerifyOnly` will confirm after promotion.

## Governance note
The corrected `install_pool.ps1` + tests + docs are on the working branch, **uncommitted**; the host now runs the
re-staged (uncommitted) validator. These must be committed → CI → merged through governance (rollback source for the
host file = `git show main:deploy/beta-agent/install_pool.ps1`).
