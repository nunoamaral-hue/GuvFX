{
  "schema_version": "1.0",
  "handoff_id": "GFX-EVD-REAL-TELEGRAM-TRANSPORT",
  "packet_id": "GFX-PKT-REAL-TELEGRAM-TRANSPORT",
  "created_at_utc": "2026-07-07T07:22:01Z",
  "branch": "feat/real-telegram-transport",
  "base_commit": "a37911bdf0ae3d4eef1c50ba63bba3e931e0ee65",
  "head_commit": "728f89b5e9af2f6c325e49063f6c611854128b6c",
  "environment": {
    "python_version": "3.11.9",
    "django_version": "5.1.2",
    "database": "PostgreSQL (local test DB)",
    "model_or_migration_change": false,
    "production_deploy": false,
    "dispatch_enabled": false,
    "transport_default": "dry-run",
    "real_message_sent": false,
    "token_committed": false
  },
  "files_added": [
    "backend/execution/notifications/real_transport.py",
    "backend/execution/tests_real_telegram_transport.py",
    "evidence/manifests/GFX-EVD-REAL-TELEGRAM-TRANSPORT.json"
  ],
  "files_modified": [
    "backend/execution/notifications/dispatcher.py",
    "backend/execution/tests_notification_transport.py"
  ],
  "commands": [
    "backend: python manage.py test execution.tests_real_telegram_transport execution.tests_notification_transport",
    "backend: python manage.py test  (full suite)",
    "backend: python manage.py makemigrations --check --dry-run execution",
    "make governance-check  (incl. secret-scan)"
  ],
  "expected_results": [
    "The DEFAULT dispatch transport stays dry-run; the real transport is used ONLY when NOTIFICATION_DISPATCH_TRANSPORT is explicitly real/telegram-real/telegram.",
    "Dispatch remains a no-op by default (NOTIFICATION_DISPATCH_ENABLED default OFF) — nothing sends unless dispatch is enabled AND the real transport is selected AND token+chat id are present.",
    "The real transport fails closed (no network call) on a missing TELEGRAM_BOT_TOKEN or TELEGRAM_CHAT_ID; refuses non-WIN outcomes.",
    "API ok=true marks SENT (transmitted=True); an HTTP/network error or ok=false marks FAILED (transmitted=False); the transport never raises.",
    "Idempotent: an already-transmitted candidate is skipped (no re-send); the dispatcher's atomic claim + SENT terminal state prevent concurrent/repeat sends.",
    "The bot token is never logged, returned in a DeliveryResult/detail, raised, or committed.",
    "real_transport.py is the ONLY network-capable file in the notifications package; it references no order_send/order_check/ExecutionJob/create_contract/deliver_trade_result/wims.",
    "No model/migration change; full backend suite green; governance + secret-scan pass."
  ],
  "actual_results": [
    "execution.tests_real_telegram_transport: 16 tests OK — selector defaults to dry-run; real only when explicitly selected (real/telegram-real/telegram; dryrun/empty/nonsense -> dry-run); missing token/chat id fail closed with urlopen NOT called; non-WIN refused (no network); API success -> ok/SENT/transmitted + urlopen called once with a Request to api.telegram.org carrying chat_id + text; HTTPError 401 -> FAILED (detail '401', no token); ok:false -> FAILED; idempotent skip when a prior transmitted delivery exists (no urlopen); dispatch disabled-by-default sends nothing (enabled False, 0 claimed, urlopen not called, 0 deliveries); enabled+real dispatch sends once + marks SENT + a second run does NOT re-send; dry-run dispatch makes no network call (transmitted False); token never appears in result.detail/repr/rendered_message or logs.",
    "execution.tests_notification_transport BoundaryTests: the no-network guard now allows ONLY real_transport.py; real_transport IS network-capable (urllib.request) and no other notifications file is; real_transport references no order/WIMS surface.",
    "Full backend suite: Ran 491 tests OK.",
    "makemigrations --check execution: No changes detected. make governance-check OK; secret-scan OK; EVIDENCE-LINT PASS.",
    "Adversarial 4-lens review (default-off-gating / token-secrecy / idempotency-failure-marking / boundary-and-winonly) + opus synthesis: recorded in the PR / reviewer field."
  ],
  "acceptance_criteria": {
    "real_transport_implemented": "PASS",
    "transport_selector_dry_run_default": "PASS",
    "env_vars_token_and_chat": "PASS",
    "safe_send_bot_api": "PASS",
    "idempotent_delivery": "PASS",
    "failure_handling": "PASS",
    "boundary_tests": "PASS",
    "dry_run_remains_default": "PASS"
  },
  "status": "PASS",
  "limitations": [
    "Repo-only. NOT deployed. NOTIFICATION_DISPATCH_ENABLED stays OFF, the default transport is dry-run, and no TELEGRAM_BOT_TOKEN/CHAT_ID are set in any tracked file — so NO real message is or can be sent by merging this. A real send requires a separate, Nuno-authorised arm step (store the token in the prod secret store + set the chat id + select the real transport + enable dispatch + deploy).",
    "Idempotency is single-delivery under normal operation (atomic PENDING->PROCESSING claim + SENT terminal state + already-transmitted skip). A narrow at-least-once window remains if the Telegram API succeeds but the DB finalize crashes before the SENT/NotificationDelivery commit (the candidate is demoted to FAILED and retried) — Telegram sendMessage has no idempotency key, so this is inherent; acceptable for the supervised demo.",
    "The token is verified only by presence/length in the secret store (never printed); no getChat/API call is made with it in this packet.",
    "TELEGRAM_CHAT_ID = @wims07072026 (the public channel username) is accepted directly by the Bot API — no numeric chat-id resolution or test send was performed (and none is performed without Nuno's explicit authorisation)."
  ],
  "artefact_locations": [
    "backend/execution/notifications/real_transport.py",
    "backend/execution/notifications/dispatcher.py",
    "backend/execution/tests_real_telegram_transport.py",
    "backend/execution/tests_notification_transport.py",
    "evidence/manifests/GFX-EVD-REAL-TELEGRAM-TRANSPORT.json"
  ],
  "checksums": {
    "backend/execution/notifications/real_transport.py": "sha256:a2cc4bb11c4820b31f5c58beee56601b446d2c5e5b13b8a2d42cbfa6cab6b5dd",
    "backend/execution/notifications/dispatcher.py": "sha256:d55d1a4d71ee3d4524de1a099870744076f02825a9bd382d7311624807af7583",
    "backend/execution/tests_notification_transport.py": "sha256:2b02023d1ccc0deeef237bbc3d1959fdfb42fbe922ca20517f96372f9b9658e2",
    "backend/execution/tests_real_telegram_transport.py": "sha256:26de25ffeefe2d607faaaf02ecf9460b680908e2511c1b531a52c61532862d46"
  },
  "reviewer": null
}
