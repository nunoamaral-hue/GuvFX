from django.apps import AppConfig


class TerminalProvisioningConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "terminal_provisioning"
    verbose_name = "Terminal Isolation Provisioning (TX-1)"
